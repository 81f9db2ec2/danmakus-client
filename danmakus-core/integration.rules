PROJECT RULES (2025-11-16)

[項目結構總覽]
1. danmakus-core
   - src/cli: CLI 入口與命令解析（Commander）。
   - src/core: 核心模組（DanmakuClient、SignalRConnection、StreamerStatusManager、CookieManager等）。
   - src/lib: npm 導出入口（對外暴露核心類與工廠函數）。
   - src/types: TS 型別定義（Config、Events、Message、StreamerConfig 等）。
   - 其他: README / DESIGN / Dockerfile / bun.lock / package.json / tsconfig。

2. danmakus-client
   - src: Vue 3 + Vite 前端（單頁控制台）。
   - src/pages: Vue 頁面（Index.vue）。
   - src-tauri: Rust 主流程與 Tauri 配置（命令、plugin、icons、tauri.conf.json 等）。
   - 其他: package.json / bun.lock / vite.config.ts / tsconfig.*。

[集成目標]
- 讓 danmakus-client 直接調用 danmakus-core（在 Tauri WebView 或 Rust 端），達成「圖形界面 + 核心彈幕采集」的一體化應用。
- 保持 Bun 環境可同時於 Node/Tauri WebView 運行核心邏輯。

[集成規劃（迭代式）]
Phase 1 · 準備與依賴管理
  1. 建立兩專案之間的本地依賴（bun workspace 或 path alias），確保 client 能 import core。
  2. 檢查 core 打包輸出（ESM/CJS），必要時調整以支援 Vite/Tauri。
  3. 在 client 內新增 service 層，封裝對 DanmakuClient 的操作（create/start/stop/status）。

Phase 2 · 基礎 UI/命令整合
  4. Tauri Rust 端：新增命令以啟動/停止 core（若採後端模式）。
  5. WebView 前端：提供啟動/停止按鈕、房間/狀態顯示，透過前述 service 呼叫 core。
  6. 設計事件橋接：將 core emit 的 msg/狀態同步至 UI（可選 signalr log、房間列表等）。

Phase 3 · 穩定化與擴展
  7. 加入錯誤提示、重試與日志面板。
  8. 支援配置管理（表單輸入 streamers、SignalR URL、Cookie Cloud 等）。
  9. 編寫基礎端到端測試或手動檢查流程，確保 GUI 與核心行為一致。

[近期 TODO 建議]
- 決定核心在 client 中的運行位置：
  (A) 直接在 WebView 內運行（利用 Bun/原生 fetch）。
  (B) 在 Tauri Rust 後端運行 Node/Bun 進程並與前端通信。
- 依選定模式落實 Phase 1 的依賴設置與 service scaffold。

[說明]
- 本 rules 文件將持續更新，以同步目前結構與集成計畫。
- 後續每次迭代請在相應 Phase 下追加或勾除項目。
